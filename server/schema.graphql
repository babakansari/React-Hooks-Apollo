type Query {
    rank(id: ID!): Rank
    crew(id: ID!): Crew
    rostering: [Crew] 
}

type Mutation {
    createCrew(name: String!, rankId: ID!): Crew
}

type Rank {
    id: ID!
    title: String!
    Crews: [Crew]
}

type Crew {
    id: ID!
    name: String!
    rank: Rank
}